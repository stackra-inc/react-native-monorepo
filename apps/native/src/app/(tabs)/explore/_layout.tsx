/**
 * Explore Tab Stack Layout
 *
 * Provides a native Stack navigator inside the Explore tab so that
 * the navigation header renders with Liquid Glass on iOS 26+.
 *
 * @module app/(tabs)/explore/_layout
 */

import { Stack } from "expo-router";

export default function ExploreLayout() {
  return <Stack />;
}
