/**
 * Settings Tab Stack Layout
 *
 * Provides a native Stack navigator inside the Settings tab so that
 * the navigation header renders with Liquid Glass on iOS 26+.
 *
 * @module app/(tabs)/settings/_layout
 */

import { Stack } from "expo-router";

export default function SettingsLayout() {
  return <Stack />;
}
