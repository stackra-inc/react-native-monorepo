/**
 * Explore Tab Route
 *
 * Thin route wrapper — delegates all UI to the ExploreScreen component.
 *
 * @module app/(tabs)/explore
 */

import { ExploreScreen } from "@/screens/explore";

export default function ExploreRoute() {
  return <ExploreScreen />;
}
