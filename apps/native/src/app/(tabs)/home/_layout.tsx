/**
 * Home Tab Stack Layout
 *
 * Provides a native Stack navigator inside the Home tab so that
 * the navigation header renders with Liquid Glass on iOS 26+.
 *
 * @module app/(tabs)/home/_layout
 */

import { Stack } from "expo-router";

export default function HomeLayout() {
  return <Stack />;
}
